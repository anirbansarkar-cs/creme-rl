from setuptools import setup, find_packages


setup(
    name="creme-rl",
    version="0.1.0",
    packages=find_packages(),
    description = "A RL package in genomics with infinite potentials ",
    python_requires=">=3.6",
    install_requires=[],
)